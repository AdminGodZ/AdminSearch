name: Update SearXNG Core

on:
  schedule:
    - cron: "0 3 * * *"
      timezone: "Europe/Zurich"
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: update-searxng-core
  cancel-in-progress: false

jobs:
  update:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Resolve latest SearXNG Docker image digest
        id: digest
        shell: bash
        run: |
          set -euo pipefail

          TOKEN="$(
            curl -fsSL \
              "https://auth.docker.io/token?service=registry.docker.io&scope=repository:searxng/searxng:pull" \
              | jq -r '.token'
          )"

          DIGEST="$(
            curl -fsSI \
              -H "Authorization: Bearer ${TOKEN}" \
              -H "Accept: application/vnd.oci.image.index.v1+json, application/vnd.docker.distribution.manifest.list.v2+json, application/vnd.oci.image.manifest.v1+json, application/vnd.docker.distribution.manifest.v2+json" \
              "https://registry-1.docker.io/v2/searxng/searxng/manifests/latest" \
              | tr -d '\r' \
              | awk -F': ' 'tolower($1) == "docker-content-digest" {print $2; exit}'
          )"

          if [ -z "${DIGEST}" ]; then
            echo "Could not resolve latest SearXNG digest" >&2
            exit 1
          fi

          if [[ ! "${DIGEST}" =~ ^sha256:[0-9a-f]{64}$ ]]; then
            echo "Docker Hub returned an invalid SearXNG digest: ${DIGEST}" >&2
            exit 1
          fi

          echo "digest=${DIGEST}" >> "$GITHUB_OUTPUT"

      - name: Track latest SearXNG digest
        shell: bash
        run: |
          set -euo pipefail

          DIGEST="${{ steps.digest.outputs.digest }}"

          sed -i -E \
            "s|^# SearXNG latest digest: sha256:[0-9a-f]{64}$|# SearXNG latest digest: ${DIGEST}|" \
            searxng/Dockerfile

          grep -Fqx "# SearXNG latest digest: ${DIGEST}" searxng/Dockerfile
          grep -Fqx "FROM docker.io/searxng/searxng:latest" searxng/Dockerfile

      - name: Test Docker build
        shell: bash
        run: |
          docker build --pull -f searxng/Dockerfile .

      - name: Commit and push if changed
        shell: bash
        run: |
          set -euo pipefail

          if git diff --quiet; then
            echo "SearXNG core image already up to date"
            exit 0
          fi

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          git add searxng/Dockerfile
          git commit -m "chore(searxng): update core image"
          git push origin "HEAD:${GITHUB_REF_NAME}"
